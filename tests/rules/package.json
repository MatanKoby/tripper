{
  "name": "tripper-firestore-rules-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Firestore security-rules unit tests (spec/access.md), run against the emulator.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-tripper 'node --test'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
