# Cloud Functions (Gen2) Python runtime dependencies for the tripper backend.
#
# This is what the Google Cloud buildpack installs into the deployed function; it mirrors
# pyproject.toml's core dependencies + the [functions] extra, plus the vendored hotel agent.
# Keep it in sync with pyproject.toml when those change.
firebase-admin>=6.5
firebase-functions>=0.4
pydantic>=2.7
pydantic-settings>=2.3

# Vendored domain agents (git submodules under vendor/, checked out with `submodules: recursive`
# in CI). Installed from their local paths so their packages are importable at runtime (each
# adapter imports its agent function-locally). Each agent's own deps resolve transitively from its
# pyproject.
#   hotel-finder  -> `hotel_finder`  (openai, httpx)
#   flight-finder -> `flight_finder` (requests, python-dotenv)
#   travel-agent  -> `travel_agent`  (langgraph + langchain stack — heavy; the largest contributor
#                    to the Function image size, spec/agents.md)
./vendor/agents/hotel-finder-agent
./vendor/agents/flight-finder-agent
./vendor/agents/travel-agent
