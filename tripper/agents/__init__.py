"""Tripper-owned adapters around the domain agents (``spec/agents.md``).

The orchestrator reaches every agent through the :class:`~tripper.agents.base.Agent` interface;
concrete adapters (M1: the hotel adapter, Batch 10) live alongside it in this package.
"""
